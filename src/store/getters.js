export const userProfile = state => state.userProfile

export const account = state => state.account

export const asset = state => state.asset

export const recordType = state => state.recordType

export const record = state => state.record
